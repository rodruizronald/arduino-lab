/*
 * WEEK 0 · EXERCISE 2 — Change the rhythm
 * ------------------------------------------------------------------
 * GOAL
 *   Same built-in LED, three different rhythms. Only the numbers
 *   change. Do them one at a time -- get each working and watch it
 *   before moving to the next.
 *
 *     A. FAST      on and off ten times per second
 *                  (work out the delay yourself: 1000 ms in a second)
 *
 *     B. SLOW      two seconds on, two seconds off
 *
 *     C. SMOKE     a short flash, then a long dark pause.
 *        DETECTOR  Like the little light on a smoke alarm.
 *
 * WIRING
 *   Still none. LED_BUILTIN.
 *
 * THE POINT
 *   You are proving to yourself that a number you type maps directly
 *   onto something you can SEE. Change number -> upload -> world
 *   changes. That loop is the engine the whole plan runs on, and it
 *   needs to feel fast and safe rather than risky.
 *
 * ------------------------------------------------------------------
 * WATCH FOR
 *   How low can the delay go before it stops looking like blinking
 *   and starts looking like a dim LED? Try 50, then 10, then 1.
 *   Your eye gives up long before the chip does -- remember that
 *   number, it explains Dry Run snippet B.
 *
 * BREAK IT
 *   Set BOTH delays to 0. What do you see? What is the LED
 *   actually doing? (It is not "off".)
 */


void setup() {
  // TODO
}

void loop() {
  // TODO: rhythm A first. Then edit for B. Then C.
}
